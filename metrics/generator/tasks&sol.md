Задача 1 
**Тема:** Переменные и типы данных  
**Сложность:** 1 (лёгкая)

**Текст задания:**
```
Вычислите общую стоимость покупки в магазине электроники. Пользователь вводит цену ноутбука, цену мыши и цену наушников (все значения — целые числа). Программа должна вывести общую сумму покупки.

**Входные данные:**  
Три целых числа, каждое с новой строки: цена ноутбука, цена мыши, цена наушников.

**Выходные данные:**  
Одно целое число — общая стоимость.

**Пример работы:**  
Входные данные:  
45000  
1200  
3500  

Выходные данные:  
49700
```

## Решение:

```c
#include <stdio.h>  // Подключение библиотеки для ввода/вывода

int main() {
    int laptop_price, mouse_price, headphones_price;  // Объявление переменных для хранения цен
    int total_cost;  // Переменная для хранения общей стоимости
    
    // Считывание цен с клавиатуры
    scanf("%d", &laptop_price);    // Чтение цены ноутбука
    scanf("%d", &mouse_price);     // Чтение цены мыши
    scanf("%d", &headphones_price); // Чтение цены наушников
    
    // Вычисление общей стоимости
    total_cost = laptop_price + mouse_price + headphones_price;
    
    // Вывод результата
    printf("%d\n", total_cost);
    
    return 0;  // Завершение программы
}
```

**Объяснение решения:**
1. Объявляются 4 переменные типа `int` для хранения цен и общей суммы
2. С помощью `scanf()` считываются три числа с клавиатуры
3. Вычисляется сумма всех цен
4. Результат выводится с помощью `printf()`

Это базовое задание демонстрирует работу с переменными целочисленного типа, вводом/выводом данных и простыми арифметическими операциями.

Задача 2
**Задание: Система скидок в спортивном клубе**

**Описание:** В спортивном клубе действует система скидок на абонемент. Базовая цена абонемента — 2000 рублей. Скидка предоставляется, если клиент соответствует одному из условий: возраст младше 18 лет ИЛИ возраст старше 60 лет. Дополнительная скидка в 10% от базовой цены предоставляется, если клиент также является ветераном спорта (это отдельный статус). Если клиент подходит под несколько условий, скидки суммируются. Рассчитайте итоговую стоимость абонемента для клиента.

**Формат входных данных:**  
Три целых числа, каждое с новой строки:  
1. Возраст клиента (положительное целое число).  
2. Статус ветерана спорта (1 — да, 0 — нет).  
3. Количество уже купленных в этом году абонементов (целое неотрицательное число).  

Если количество уже купленных абонементов в этом году больше 3, предоставляется дополнительная фиксированная скидка в 200 рублей (эта скидка применяется после всех процентных скидок).

**Формат выходных данных:**  
Одно целое число — итоговая стоимость абонемента в рублях (округление стандартное для целых чисел, проценты считаются от базовой цены).

**Пример 1:**  
Входные данные:  
25  
0  
2  
Выходные данные:  
2000  

**Пример 2:**  
Входные данные:  
65  
1  
5  
Выходные данные:  
1400  

---

## **Решение на языке C:**

```c
#include <stdio.h>

int main() {
    int age, veteran_status, purchased_count;
    int base_price = 2000;
    int final_price;
    int discount_percent = 0;
    
    // Чтение входных данных
    scanf("%d", &age);
    scanf("%d", &veteran_status);
    scanf("%d", &purchased_count);
    
    // Начальная цена равна базовой
    final_price = base_price;
    
    // Проверка возрастной скидки (младше 18 ИЛИ старше 60)
    if (age < 18 || age > 60) {
        discount_percent += 10; // 10% скидка за возраст
    }
    
    // Проверка статуса ветерана спорта
    if (veteran_status == 1) {
        discount_percent += 10; // 10% скидка за статус ветерана
    }
    
    // Применение процентных скидок
    if (discount_percent > 0) {
        // Вычисляем сумму скидки в рублях
        int discount_amount = base_price * discount_percent / 100;
        final_price = base_price - discount_amount;
    }
    
    // Применение фиксированной скидки за количество абонементов
    if (purchased_count > 3) {
        final_price -= 200; // Вычитаем 200 рублей
    }
    
    // Вывод итоговой стоимости
    printf("%d\n", final_price);
    
    return 0;
}
```

**Объяснение решения:**
1. Считываем три входных параметра: возраст, статус ветерана и количество купленных абонементов
2. Проверяем возрастное условие с помощью оператора `||` (ИЛИ)
3. Проверяем статус ветерана
4. Суммируем процентные скидки и вычисляем сумму скидки в рублях
5. Применяем фиксированную скидку за количество абонементов (если больше 3)
6. Выводим итоговую стоимость

Задание охватывает различные аспекты условных операторов: простые условия, составные условия с логическими операторами, последовательную проверку нескольких условий и вычисления на основе условий.

Задача 3
## **Задание: Определение зачётного результата в прыжках в длину**

**Описание:**  
В спортивном соревновании по прыжках в длину результаты измеряются в сантиметрах. Участник выполняет три попытки. Судьи определяют зачётную попытку по следующему правилу:  
- Если все три попытки успешны (результат больше 0), то зачётной считается лучшая попытка.  
- Если ровно две попытки успешны, то зачётной считается худшая из успешных попыток.  
- Если успешна только одна попытка, она и становится зачётной.  
- Если все попытки неудачны (результат 0 или отрицательный), зачётный результат равен 0.  

Напишите программу, которая по трём целым числам (результатам попыток) определяет зачётный результат. Используйте только условные операторы (ветвления). Циклы, массивы, функции не применяйте.

**Формат входных данных:**  
Три целых числа (тип `int`), каждое на новой строке — результаты трёх попыток в сантиметрах.

**Формат выходных данных:**  
Одно целое число — зачётный результат.

**Примеры работы:**  
Вход:  
500  
520  
510  
Выход:  
520  

Вход:  
0  
450  
480  
Выход:  
450  

Вход:  
-5  
0  
0  
Выход:  
0

---

## **Решение:**

```c
#include <stdio.h>

int main() {
    int a, b, c;
    int result = 0; // Зачётный результат, по умолчанию 0

    // Чтение трёх попыток
    scanf("%d", &a);
    scanf("%d", &b);
    scanf("%d", &c);

    // Подсчёт количества успешных попыток (результат > 0)
    int successful = 0;
    if (a > 0) successful++;
    if (b > 0) successful++;
    if (c > 0) successful++;

    // Определение зачётного результата по правилам
    if (successful == 3) {
        // Лучшая из трёх успешных попыток
        result = a;
        if (b > result) result = b;
        if (c > result) result = c;
    } else if (successful == 2) {
        // Худшая из двух успешных попыток
        if (a > 0 && b > 0) {
            result = (a < b) ? a : b;
        } else if (a > 0 && c > 0) {
            result = (a < c) ? a : c;
        } else { // b > 0 && c > 0
            result = (b < c) ? b : c;
        }
    } else if (successful == 1) {
        // Единственная успешная попытка
        if (a > 0) result = a;
        else if (b > 0) result = b;
        else result = c;
    }
    // Если successful == 0, result остаётся 0

    // Вывод результата
    printf("%d\n", result);
    return 0;
}
```

**Особенности задания (сложность 3):**
1. Требуется анализировать не только значения, но и количество успешных попыток
2. Разные правила для разного количества успешных попыток
3. Необходимо обрабатывать граничные случаи (0 и отрицательные значения)
4. Использование только условных операторов без циклов и массивов
5. Логика определения "лучшей" и "худшей" попытки в зависимости от контекста

Задача 4

**Задание: Расчет оптимального времени тренировки**

**Тема:** Циклы (сложность 3)

**Условие задачи:**
В спортивной секции тренер разрабатывает план тренировок. Для каждого спортсмена рассчитывается оптимальное время тренировки в минутах по следующему правилу: начальное время равно 30 минутам. Каждую последующую тренировку время увеличивается на 5 минут, но не должно превышать лимит в 120 минут. Однако, если номер тренировки кратен 4, то время не меняется (пропуск увеличения). Необходимо написать программу, которая для заданного номера тренировки N (N ≥ 1) вычисляет и выводит оптимальное время для этой тренировки.

**Формат входных данных:**
Одно целое число N (1 ≤ N ≤ 1000) — номер тренировки.

**Формат выходных данных:**
Одно целое число — оптимальное время тренировки в минутах.

**Пример 1:**
Входные данные: `3`
Выходные данные: `40`
*Пояснение:* 1-я тренировка: 30 мин, 2-я: 30+5=35 мин, 3-я: 35+5=40 мин.

**Пример 2:**
Входные данные: `8`
Выходные данные: `55`
*Пояснение:* 4-я и 8-я тренировки — кратные 4, время на них не увеличивается. Расчет: 30→35→40→40→45→50→55→55.

---

## **Решение на языке C:**

```c
#include <stdio.h>

int main() {
    int N;
    // Считываем номер тренировки
    scanf("%d", &N);
    
    int time = 30; // начальное время для первой тренировки
    int limit = 120; // максимально допустимое время
    
    // Проходим по всем тренировкам от 1 до N
    for (int i = 1; i <= N; i++) {
        // Если это не первая тренировка и номер не кратен 4
        if (i > 1 && i % 4 != 0) {
            // Увеличиваем время, но не превышаем лимит
            if (time + 5 <= limit) {
                time += 5;
            } else {
                time = limit; // если превысили, устанавливаем лимит
            }
        }
        // Если номер кратен 4, время не меняется (пропускаем увеличение)
    }
    
    // Выводим результат
    printf("%d\n", time);
    
    return 0;
}
```

**Алгоритм решения:**
1. Инициализируем начальное время 30 минут и лимит 120 минут
2. Используем цикл `for` для прохода по всем тренировкам от 1 до N
3. Для каждой тренировки проверяем:
   - Если это не первая тренировка И номер не кратен 4 → увеличиваем время на 5 минут
   - Проверяем, не превышает ли новое время лимит
4. Если номер тренировки кратен 4 → пропускаем увеличение времени
5. Выводим итоговое время для N-й тренировки

**Особенности задачи (сложность 3):**
- Сочетание цикла с условными операторами
- Обработка граничных случаев (кратность 4, лимит времени)
- Необходимость отслеживать состояние переменной через все итерации цикла
- Логика пропуска увеличения на определенных шагах

Задача 5

**Тема:** Массивы  
**Сложность:** Лёгкая (1)

**Описание:** В спортивном зале проводятся соревнования по прыжкам в длину. Участники выполняют по три попытки. Программа должна вычислить лучший результат каждого участника (максимальную длину прыжка из трех попыток). Входные данные: количество участников N, затем для каждого участника три результата прыжков в метрах (вещественные числа). Выходные данные: лучший результат каждого участка, выведенный с точностью до двух знаков после запятой.

**Формат входных данных:**  
Первая строка содержит целое число N (1 ≤ N ≤ 100) — количество участников.  
Далее идут N строк, в каждой строке три вещественных числа через пробел — результаты трех попыток соответствующего участника.

**Формат выходных данных:**  
N строк, в каждой строке одно вещественное число — лучший результат соответствующего участника, выведенный с точностью до двух знаков после запятой.

**Пример работы:**  
Входные данные:  
3  
5.80 5.95 5.70  
6.10 6.05 6.20  
5.50 5.60 5.55  

Выходные данные:  
5.95  
6.20  
5.60

---

## Решение на языке C:

```c
#include <stdio.h>

int main() {
    int n; // Количество участников
    scanf("%d", &n); // Чтение количества участников
    
    // Обработка данных для каждого участника
    for (int i = 0; i < n; i++) {
        double jump1, jump2, jump3; // Результаты трех попыток
        scanf("%lf %lf %lf", &jump1, &jump2, &jump3); // Чтение трех попыток
        
        // Находим максимальный результат из трех попыток
        double best = jump1; // Предполагаем, что первая попытка лучшая
        if (jump2 > best) {
            best = jump2; // Вторая попытка лучше
        }
        if (jump3 > best) {
            best = jump3; // Третья попытка лучше
        }
        
        // Выводим лучший результат с точностью до двух знаков после запятой
        printf("%.2f\n", best);
    }
    
    return 0;
}


Задача 6
## 📝 Задание по массивам (сложность 2)

**Тема:** Массивы  
**Сложность:** Средняя (2)

**Описание:** В спортивном соревновании по прыжкам в длину каждый участник выполняет три попытки. Результаты каждой попытки измеряются в сантиметрах (целые числа). Программа должна для каждого участника определить его лучший результат (максимальный из трёх попыток) и вывести эти лучшие результаты для всех участников в том же порядке.

**Входные данные:** В первой строке вводится целое число N (1 ≤ N ≤ 100) — количество участников. Далее следуют N строк, в каждой из которых через пробел записаны три целых числа — результаты трёх попыток соответствующего участника.

**Выходные данные:** Выведите N целых чисел через пробел — лучший результат каждого участника.

**Пример работы:**
```
Вход:
3
255 240 260
290 285 288
270 275 265

Выход:
260 290 275
```

## 💻 Решение на языке C

```c
#include <stdio.h>

int main() {
    int N; // Количество участников
    scanf("%d", &N); // Чтение количества участников
    
    // Массив для хранения лучших результатов каждого участника
    int best_results[100]; // Максимум 100 участников
    
    // Обработка данных для каждого участника
    for (int i = 0; i < N; i++) {
        int a, b, c; // Результаты трех попыток
        scanf("%d %d %d", &a, &b, &c); // Чтение трех попыток
        
        // Находим максимальный результат среди трех попыток
        int max = a; // Предполагаем, что первая попытка лучшая
        if (b > max) {
            max = b; // Вторая попытка лучше
        }
        if (c > max) {
            max = c; // Третья попытка лучше
        }
        
        // Сохраняем лучший результат для текущего участника
        best_results[i] = max;
    }
    
    // Вывод лучших результатов всех участников
    for (int i = 0; i < N; i++) {
        printf("%d", best_results[i]); // Вывод результата
        if (i < N - 1) {
            printf(" "); // Пробел между числами, кроме последнего
        }
    }
    
    return 0;
}
```

## 🔑 Ключевые моменты решения:

1. **Использование массива** `best_results[100]` для хранения лучших результатов каждого участника
2. **Чтение данных** в цикле для каждого участника
3. **Поиск максимума** из трёх попыток с помощью простых сравнений
4. **Сохранение результатов** в массив для последующего вывода
5. **Форматированный вывод** с пробелами между числами

Это задание хорошо демонстрирует работу с массивами, циклами и условными операторами, что соответствует средней сложности.

Задача 7


## 📋 Задание: Наибольший неухудшающийся участок

**Тема:** Массивы  
**Сложность:** 3 (сложное)

**Описание:** В спортивном турнире по стрельбе из лука каждый участник делает серию из N выстрелов. Результаты каждого выстрела (количество очков) записываются в массив целых чисел. Для анализа стабильности стрелка необходимо найти длину наибольшего непрерывного участка (подмассива), на котором результаты не ухудшались (каждый следующий выстрел был не хуже предыдущего, т.е. количество очков было больше или равно). Напишите программу, которая по массиву результатов выстрелов определяет длину такого участка.

**Формат входных данных:**  
В первой строке задано целое число N (1 ≤ N ≤ 1000) — количество выстрелов.  
Во второй строке через пробел заданы N целых чисел — результаты каждого выстрела (от 0 до 10).

**Формат выходных данных:**  
Одно целое число — длина наибольшего непрерывного участка, на котором результаты не ухудшались.

**Пример 1:**  
Входные данные:
```
8
5 7 7 8 6 9 9 10
```
Выходные данные:
```
4
```
Пояснение: Самый длинный неухудшающийся участок — это первые четыре выстрела: 5, 7, 7, 8.

**Пример 2:**  
Входные данные:
```
5
10 10 10 10 10
```
Выходные данные:
```
5
```
Пояснение: Все результаты одинаковы, весь массив является неухудшающимся участком.

---

## 💻 Решение на языке C:

```c
#include <stdio.h>

int main() {
    int N;
    // Считываем количество выстрелов
    scanf("%d", &N);
    
    int results[1000]; // Массив для хранения результатов выстрелов
    // Считываем результаты выстрелов
    for (int i = 0; i < N; i++) {
        scanf("%d", &results[i]);
    }
    
    int max_length = 1; // Минимальная длина участка - 1 выстрел
    int current_length = 1; // Длина текущего неухудшающегося участка
    
    // Проходим по массиву, начиная со второго элемента
    for (int i = 1; i < N; i++) {
        // Если текущий результат не хуже предыдущего
        if (results[i] >= results[i-1]) {
            current_length++; // Увеличиваем длину текущего участка
        } else {
            // Если нашли ухудшение, сбрасываем счетчик
            current_length = 1;
        }
        
        // Обновляем максимальную длину, если текущая больше
        if (current_length > max_length) {
            max_length = current_length;
        }
    }
    
    // Выводим результат
    printf("%d\n", max_length);
    
    return 0;
}

Задача 8
user_request = "Создай задание на тему '5' (Функции) сложности 2 и решение к нему"
Задание: Расчет стоимости поездки на такси с учетом скидок

**Тема:** Функции (сложность 2)

**Задача:** Написать программу для расчета стоимости поездки на такси с системой скидок, используя три функции:
1. `calculateBaseCost` - расчет базовой стоимости
2. `applyLongTripDiscount` - применение скидки за длинную поездку (15% при расстоянии > 20 км)
3. `applyLoyaltyDiscount` - применение скидки постоянного клиента

**Входные данные:** Четыре целых числа: расстояние (км), тариф (руб/км), скидка постоянного клиента (руб), флаг (0 или 1).

**Выходные данные:** Итоговая стоимость поездки.

## Решение:

```c
#include <stdio.h>

// Функция для расчета базовой стоимости
int calculateBaseCost(int distance, int tariff) {
    return distance * tariff;
}

// Функция для применения скидки за длинную поездку
int applyLongTripDiscount(int baseCost, int distance) {
    if (distance > 20) {
        // Применяем скидку 15% (умножаем на 0.85)
        return (int)(baseCost * 0.85);
    }
    return baseCost;
}

// Функция для применения скидки постоянного клиента
int applyLoyaltyDiscount(int costAfterFirstDiscount, int loyaltyDiscount) {
    int finalCost = costAfterFirstDiscount - loyaltyDiscount;
    if (finalCost < 0) {
        return 0;
    }
    return finalCost;
}

int main() {
    int distance, tariff, loyaltyDiscount, flag;
    
    // Считываем входные данные
    scanf("%d %d %d %d", &distance, &tariff, &loyaltyDiscount, &flag);
    
    // 1. Рассчитываем базовую стоимость
    int baseCost = calculateBaseCost(distance, tariff);
    
    // 2. Применяем скидку за длинную поездку (если применимо)
    int costAfterLongTrip = applyLongTripDiscount(baseCost, distance);
    
    // 3. Применяем скидку постоянного клиента только если flag == 1
    int finalCost;
    if (flag == 1) {
        finalCost = applyLoyaltyDiscount(costAfterLongTrip, loyaltyDiscount);
    } else {
        finalCost = costAfterLongTrip;
    }
    
    // Выводим результат
    printf("%d\n", finalCost);
    
    return 0;
}

Задание 9

**Тема:** Функции (сложность 3)

**Описание задания:**
Разработайте программу для расчета сложных процентов с учетом различных условий начисления. Программа должна использовать функции для вычисления итоговой суммы в зависимости от типа начисления (ежегодное, ежеквартальное, ежемесячное) и проверки корректности входных данных.

**Требуемые функции:**
1. `int validateInput(double principal, double rate, int years)` — проверяет корректность входных данных
2. `double calculateAnnual(double principal, double rate, int years)` — вычисляет итоговую сумму при ежегодном начислении
3. `double calculateQuarterly(double principal, double rate, int years)` — вычисляет итоговую сумму при ежеквартальном начислении
4. `double calculateMonthly(double principal, double rate, int years)` — вычисляет итоговую сумму при ежемесячном начислении
5. `void printResults(double annual, double quarterly, double monthly)` — выводит результаты

**Формат входных данных:**
Три числа типа `double` в одной строке через пробел: начальный вклад, годовая процентная ставка, срок в годах.

**Формат выходных данных:**
Если данные корректны:
```
Annual: <сумма>
Quarterly: <сумма>
Monthly: <сумма>
```
Если данные некорректны:
```
Invalid input
```

**Примеры:**
```
Вход: 1000 5 2
Выход:
Annual: 1102.50
Quarterly: 1104.49
Monthly: 1104.94

Вход: -500 3 1
Выход: Invalid input
```

## **Решение:**

```c
#include <stdio.h>
#include <math.h>

// Функция проверки корректности входных данных
int validateInput(double principal, double rate, int years) {
    // Проверяем, что начальный вклад > 0, ставка >= 0, срок > 0
    if (principal > 0 && rate >= 0 && years > 0) {
        return 1; // Данные корректны
    } else {
        return 0; // Данные некорректны
    }
}

// Функция расчета для ежегодного начисления процентов
double calculateAnnual(double principal, double rate, int years) {
    // Формула сложных процентов: A = P * (1 + r/100)^t
    return principal * pow(1 + rate / 100, years);
}

// Функция расчета для ежеквартального начисления процентов
double calculateQuarterly(double principal, double rate, int years) {
    // Начисление 4 раза в год: A = P * (1 + r/100/4)^(t*4)
    return principal * pow(1 + rate / 100 / 4, years * 4);
}

// Функция расчета для ежемесячного начисления процентов
double calculateMonthly(double principal, double rate, int years) {
    // Начисление 12 раз в год: A = P * (1 + r/100/12)^(t*12)
    return principal * pow(1 + rate / 100 / 12, years * 12);
}

// Функция вывода результатов
void printResults(double annual, double quarterly, double monthly) {
    // Вывод с двумя знаками после запятой
    printf("Annual: %.2f\n", annual);
    printf("Quarterly: %.2f\n", quarterly);
    printf("Monthly: %.2f\n", monthly);
}

int main() {
    double principal, rate;
    int years;
    
    // Считывание входных данных
    scanf("%lf %lf %d", &principal, &rate, &years);
    
    // Проверка корректности данных
    if (!validateInput(principal, rate, years)) {
        printf("Invalid input\n");
        return 0; // Завершение программы при некорректных данных
    }
    
    // Расчет для разных типов начисления процентов
    double annual = calculateAnnual(principal, rate, years);
    double quarterly = calculateQuarterly(principal, rate, years);
    double monthly = calculateMonthly(principal, rate, years);
    
    // Вывод результатов
    printResults(annual, quarterly, monthly);
    
    return 0;
}

Задача 10

**Тема:** Указатели  
**Сложность:** Лёгкая (1)

**Описание:**  
В спортивной аналитике необходимо отслеживать количество голов, забитых футбольной командой в каждом матче. Напишите программу, которая использует указатель для работы с переменной, хранящей количество голов в текущем матче. Программа должна считывать количество голов, увеличивать его на 1 (засчитать дополнительный гол) и выводить обновленное значение.

**Формат входных данных:**  
Одно целое число — начальное количество голов в матче.

**Формат выходных данных:**  
Одно целое число — обновленное количество голов после добавления одного гола.

**Пример работы:**  
Входные данные:  
3  
Выходные данные:  
4

---

## Решение:

```c
#include <stdio.h>  // Для функций ввода/вывода

int main() {
    int goals;  // Переменная для хранения количества голов
    int *ptr;   // Указатель на целое число
    
    // Считываем начальное количество голов
    scanf("%d", &goals);
    
    // Присваиваем указателю адрес переменной goals
    ptr = &goals;
    
    // Увеличиваем значение через указатель на 1
    (*ptr)++;
    
    // Выводим обновленное количество голов
    printf("%d\n", goals);
    
    return 0;
}
```

**Объяснение решения:**
1. Объявляем переменную `goals` для хранения количества голов
2. Объявляем указатель `ptr` типа `int*`
3. Считываем начальное значение с помощью `scanf()`
4. Присваиваем указателю адрес переменной `goals` с помощью оператора `&`
5. Увеличиваем значение через указатель с помощью операции `(*ptr)++`
6. Выводим обновленное значение переменной `goals`

**Ключевые моменты:**
- Использование оператора `&` для получения адреса переменной
- Использование оператора `*` для доступа к значению по адресу
- Изменение значения переменной через указатель

Задача 11

## **Задание: Средневзвешенная цена акций с использованием указателей**

**Тема:** Указатели (сложность 3)

**Описание:** В контексте финансового анализа необходимо вычислить средневзвешенную цену акции на основе нескольких сделок. Каждая сделка характеризуется ценой (тип float) и объемом (тип int). Используйте указатели для обхода данных о сделках, хранящихся в отдельных переменных. Рассчитайте общую стоимость всех сделок (цена * объем) и общий объем, а затем найдите средневзвешенную цену. Все вычисления производите исключительно через указатели, без использования оператора взятия адреса (&) для уже объявленных переменных, кроме инициализации указателей.

**Формат входных данных:** Программа не принимает входные данные. Исходные данные о сделках (цена и объем) заданы в виде инициализированных переменных в коде.

**Формат выходных данных:** Вывести одно вещественное число — средневзвешенную цену с точностью до двух знаков после запятой.

**Пример работы:**
Пусть в коде заданы переменные:
price1 = 150.5, volume1 = 10;
price2 = 152.0, volume2 = 5;
price3 = 149.75, volume3 = 8;

Программа должна вычислить:
Общая стоимость = (150.5 × 10) + (152.0 × 5) + (149.75 × 8) = 1505 + 760 + 1198 = 3463
Общий объем = 10 + 5 + 8 = 23
Средневзвешенная цена = 3463 / 23 ≈ 150.57

Вывод:
150.57

---

## **Решение:**

```c
#include <stdio.h>

int main() {
    // Исходные данные о сделках
    float price1 = 150.5, price2 = 152.0, price3 = 149.75;
    int volume1 = 10, volume2 = 5, volume3 = 8;

    // Указатели на переменные с ценами
    float *p_price1 = &price1;
    float *p_price2 = &price2;
    float *p_price3 = &price3;

    // Указатели на переменные с объемами
    int *p_volume1 = &volume1;
    int *p_volume2 = &volume2;
    int *p_volume3 = &volume3;

    // Массивы указателей на указатели для удобного обхода
    float **price_ptrs[] = {&p_price1, &p_price2, &p_price3};
    int **volume_ptrs[] = {&p_volume1, &p_volume2, &p_volume3};

    // Переменные для накопления сумм
    float total_cost = 0.0;
    int total_volume = 0;

    // Обход сделок через указатели
    for (int i = 0; i < 3; i++) {
        // Получаем текущие указатели на цену и объем
        float *current_price_ptr = *price_ptrs[i];
        int *current_volume_ptr = *volume_ptrs[i];

        // Вычисляем стоимость сделки и добавляем к общей стоимости
        total_cost += (*current_price_ptr) * (*current_volume_ptr);
        
        // Добавляем объем к общему объему
        total_volume += *current_volume_ptr;
    }

    // Вычисляем средневзвешенную цену
    float weighted_average_price = total_cost / total_volume;

    // Вывод результата с точностью до двух знаков после запятой
    printf("%.2f\n", weighted_average_price);

    return 0;
}

Задача 12

**Тема:** Структуры данных  
**Название:** Учет книг в библиотеке

**Описание задачи:**
Создайте структуру `Book`, которая хранит информацию о книге: название (массив символов), автора (массив символов) и год издания (целое число). В программе объявите одну переменную этого типа, инициализируйте ее данными, введенными с клавиатуры, и выведите информацию о книге в формате: "Название: [название], Автор: [автор], Год: [год]".

**Формат входных данных:**
Три строки, разделенные переводом строки:
1. Название книги (до 100 символов).
2. Автор книги (до 100 символов).
3. Год издания (целое число).

**Формат выходных данных:**
Одна строка в формате: "Название: [название], Автор: [автор], Год: [год]".

**Пример работы:**
```
Входные данные:
Война и мир
Лев Толстой
1869

Выходные данные:
Название: Война и мир, Автор: Лев Толстой, Год: 1869
```

## Решение на языке C:

```c
#include <stdio.h>
#include <string.h>

// Структура для хранения информации о книге
struct Book {
    char title[101];    // Название книги (до 100 символов + '\0')
    char author[101];   // Автор книги (до 100 символов + '\0')
    int year;           // Год издания
};

int main() {
    struct Book book;  // Объявляем переменную типа Book
    
    // Ввод названия книги
    fgets(book.title, sizeof(book.title), stdin);
    // Удаляем символ новой строки в конце строки
    book.title[strcspn(book.title, "\n")] = '\0';
    
    // Ввод автора книги
    fgets(book.author, sizeof(book.author), stdin);
    // Удаляем символ новой строки в конце строки
    book.author[strcspn(book.author, "\n")] = '\0';
    
    // Ввод года издания
    scanf("%d", &book.year);
    
    // Вывод информации о книге в требуемом формате
    printf("Название: %s, Автор: %s, Год: %d\n", 
           book.title, book.author, book.year);
    
    return 0;
}
```

**Краткое объяснение решения:**
1. Создана структура `Book` с тремя полями: `title`, `author` и `year`.
2. В функции `main()` объявлена переменная `book` типа `struct Book`.
3. Для ввода строк используется `fgets()`, который безопасно читает строки с пробелами.
4. Функция `strcspn()` используется для удаления символа новой строки из введенных строк.
5. Для ввода года используется `scanf()`.
6. Вывод осуществляется с помощью `printf()` в требуемом формате.

Это задание идеально подходит для знакомства с основами структур в языке C.

Задача 13



## **Задание: Учет книг в библиотеке**

**Тема:** Структуры данных (7)
**Сложность:** Средняя (2)

**Описание задачи:**
В библиотеке ведется учет книг. Для каждой книги хранится следующая информация: название (строка до 100 символов), автор (строка до 100 символов), год издания (целое число), количество страниц (целое число) и признак наличия в читальном зале (символ 'Y' — есть, 'N' — нет).

Напишите программу, которая:
1. Объявляет структуру `Book` для хранения информации об одной книге.
2. В функции `main` создает массив из 3 структур `Book`.
3. Заполняет данные для каждой книги, считывая их с клавиатуры в указанном порядке: название, автор, год, страницы, наличие.
4. Находит и выводит название и автора самой старой книги (с наименьшим годом издания). Если таких книг несколько, выведите данные первой из них.
5. Подсчитывает и выводит общее количество страниц во всех книгах, которые есть в читальном зале (признак наличия равен 'Y').

**Формат входных данных:**
Для каждой из трех книг в отдельной строке:
- Название книги (одно слово или несколько слов без переноса строки внутри)
- Автор (одно слово или несколько слов без переноса строки внутри)
- Год издания (целое число)
- Количество страниц (целое число)
- Признак наличия в читальном зале (один символ: Y или N)

**Формат выходных данных:**
- Первая строка: название и автор самой старой книги, разделенные пробелом
- Вторая строка: общее количество страниц в доступных книгах

**Пример работы программы:**

Входные данные:
```
The Catcher in the Rye
J.D. Salinger
1951
277
Y
Nineteen Eighty-Four
George Orwell
1949
328
Y
Brave New World
Aldous Huxley
1932
311
N
```

Выходные данные:
```
Brave New World Aldous Huxley
605
```

---

## **Решение:**

```c
#include <stdio.h>
#include <string.h>

// Структура для хранения информации о книге
struct Book {
    char title[101];      // Название (до 100 символов + '\0')
    char author[101];     // Автор (до 100 символов + '\0')
    int year;             // Год издания
    int pages;            // Количество страниц
    char available;       // Признак наличия в читальном зале (Y/N)
};

int main() {
    struct Book books[3]; // Массив из 3 книг
    
    // 1. Ввод данных для каждой книги
    for (int i = 0; i < 3; i++) {
        // Считываем название (можно с пробелами)
        fgets(books[i].title, 101, stdin);
        // Убираем символ новой строки в конце
        books[i].title[strcspn(books[i].title, "\n")] = '\0';
        
        // Считываем автора
        fgets(books[i].author, 101, stdin);
        books[i].author[strcspn(books[i].author, "\n")] = '\0';
        
        // Считываем год издания
        scanf("%d", &books[i].year);
        
        // Считываем количество страниц
        scanf("%d", &books[i].pages);
        
        // Считываем признак наличия
        scanf(" %c", &books[i].available); // Пробел перед %c игнорирует перенос строки
        
        // Очищаем буфер ввода после scanf
        while (getchar() != '\n');
    }
    
    // 2. Поиск самой старой книги
    int oldest_index = 0; // Индекс самой старой книги
    for (int i = 1; i < 3; i++) {
        if (books[i].year < books[oldest_index].year) {
            oldest_index = i;
        }
    }
    
    // 3. Подсчет общего количества страниц доступных книг
    int total_pages = 0;
    for (int i = 0; i < 3; i++) {
        if (books[i].available == 'Y') {
            total_pages += books[i].pages;
        }
    }
    
    // 4. Вывод результатов
    printf("%s %s\n", books[oldest_index].title, books[oldest_index].author);
    printf("%d\n", total_pages);
    
    return 0;
}
```

**Ключевые моменты решения:**
1. Используется структура `Book` для хранения всех данных о книге
2. Для ввода строк с пробелами используется `fgets()` вместо `scanf()`
3. Функция `strcspn()` используется для удаления символа новой строки
4. Поиск самой старой книги осуществляется простым сравнением годов
5. Подсчет страниц доступных книг выполняется с проверкой признака наличия 'Y'

Задача 14
## Задание по структурам данных (сложность 3)

**Текст задания:**
В спортивной аналитике данные о матчах хранятся в виде структур. Создайте структуру `Match`, содержащую поля: название первой команды (строка), название второй команды (строка), счет первой команды (целое число), счет второй команды (целое число), общее количество зрителей (целое число).

Напишите программу, которая считывает данные о N матчах. Программа должна определить и вывести название команды, которая чаще всего играла в матчах с общим количеством голов больше 5. Если таких команд несколько, выведите ту, у которой суммарное количество зрителей на всех её матчах больше. Если и по этому критерию есть равенство, выведите любую из них.

**Формат входных данных:**
Первая строка содержит целое число N (1 ≤ N ≤ 1000) — количество матчей.
Следующие N строк содержат данные об одном матче в формате: "Команда1 Команда2 Счет1 Счет2 Зрители". Все названия команд состоят из одного слова без пробелов.

**Формат выходных данных:**
Одна строка — название искомой команды.

**Пример работы:**
Входные данные:
```
5
Alpha Beta 3 4 12000
Gamma Alpha 2 2 8000
Delta Gamma 5 1 15000
Beta Epsilon 0 0 5000
Alpha Delta 4 3 11000
```

Выходные данные:
```
Alpha
```

## Решение на языке C:

```c
#include <stdio.h>
#include <string.h>

// Структура для хранения данных о матче
struct Match {
    char team1[51];   // название первой команды
    char team2[51];   // название второй команды
    int score1;       // счет первой команды
    int score2;       // счет второй команды
    int spectators;   // количество зрителей
};

int main() {
    int N;
    scanf("%d", &N);
    
    struct Match matches[1000];
    
    // Чтение данных о матчах
    for (int i = 0; i < N; i++) {
        scanf("%s %s %d %d %d", 
              matches[i].team1, 
              matches[i].team2, 
              &matches[i].score1, 
              &matches[i].score2, 
              &matches[i].spectators);
    }
    
    // Массивы для хранения статистики по командам
    char teams[2000][51];  // список уникальных команд
    int match_count[2000] = {0};  // количество подходящих матчей
    int total_spectators[2000] = {0};  // суммарное количество зрителей
    int team_count = 0;  // количество уникальных команд
    
    // Обработка каждого матча
    for (int i = 0; i < N; i++) {
        // Проверяем, подходит ли матч (общее количество голов > 5)
        int total_goals = matches[i].score1 + matches[i].score2;
        if (total_goals > 5) {
            // Обрабатываем обе команды из матча
            char* team_names[2] = {matches[i].team1, matches[i].team2};
            
            for (int j = 0; j < 2; j++) {
                char* current_team = team_names[j];
                int found = 0;
                
                // Ищем команду в списке уникальных команд
                for (int k = 0; k < team_count; k++) {
                    if (strcmp(teams[k], current_team) == 0) {
                        // Команда найдена, обновляем статистику
                        match_count[k]++;
                        total_spectators[k] += matches[i].spectators;
                        found = 1;
                        break;
                    }
                }
                
                // Если команда не найдена, добавляем её
                if (!found) {
                    strcpy(teams[team_count], current_team);
                    match_count[team_count] = 1;
                    total_spectators[team_count] = matches[i].spectators;
                    team_count++;
                }
            }
        }
    }
    
    // Поиск команды с максимальным количеством подходящих матчей
    char best_team[51] = "";
    int max_matches = -1;
    int max_spectators = -1;
    
    for (int i = 0; i < team_count; i++) {
        // Если нашли команду с большим количеством матчей
        if (match_count[i] > max_matches) {
            max_matches = match_count[i];
            max_spectators = total_spectators[i];
            strcpy(best_team, teams[i]);
        }
        // Если количество матчей одинаковое, сравниваем по зрителям
        else if (match_count[i] == max_matches) {
            if (total_spectators[i] > max_spectators) {
                max_spectators = total_spectators[i];
                strcpy(best_team, teams[i]);
            }
        }
    }
    
    // Вывод результата
    printf("%s\n", best_team);
    
    return 0;
}
```

**Ключевые моменты решения:**
1. Используется структура `Match` для хранения данных о каждом матче
2. Программа читает N матчей и сохраняет их в массив структур
3. Для каждой команды ведется статистика: количество матчей с >5 голов и суммарное количество зрителей
4. Реализована логика поиска уникальных команд и обновления их статистики
5. Алгоритм находит команду с максимальным количеством подходящих матчей, а при равенстве выбирает по количеству зрителей
6. Программа готова к компиляции и запуску

Задача 15

## **Задание: Среднее количество посетителей спортивного клуба**

**Описание:** В спортивном клубе ведется учет количества посетителей в разные дни недели. Для удобства анализа данных определите макрос, который вычисляет среднее количество посетителей за неделю. Используйте директивы препроцессора для определения констант — количества дней в неделе и названия клуба. Программа должна выводить название клуба и рассчитанное среднее значение.

**Формат входных данных:** Семь целых чисел через пробел — количество посетителей в понедельник, вторник, среду, четверг, пятницу, субботу и воскресенье.

**Формат выходных данных:** Две строки. В первой строке выведите название клуба. Во второй строке выведите среднее количество посетителей за неделю, округленное до двух знаков после запятой.

**Пример работы:**
Входные данные:
```
120 135 110 145 160 200 180
```

Выходные данные:
```
Спортивный клуб "Энергия"
150.00
```

## **Решение:**

```c
#include <stdio.h>   // Для ввода/вывода
#include <stdlib.h>  // Для функции exit

// Определяем макрос для вычисления среднего количества посетителей
#define AVERAGE_VISITORS(v1, v2, v3, v4, v5, v6, v7) \
    ((v1 + v2 + v3 + v4 + v5 + v6 + v7) / (float)DAYS_IN_WEEK)

// Определяем константы через директивы препроцессора
#define DAYS_IN_WEEK 7
#define CLUB_NAME "Спортивный клуб \"Энергия\""

int main() {
    int mon, tue, wed, thu, fri, sat, sun;
    
    // Считываем количество посетителей за каждый день
    if (scanf("%d %d %d %d %d %d %d", 
              &mon, &tue, &wed, &thu, &fri, &sat, &sun) != 7) {
        printf("Ошибка ввода данных\n");
        exit(1);  // Завершаем программу при ошибке ввода
    }
    
    // Вычисляем среднее значение с помощью макроса
    float average = AVERAGE_VISITORS(mon, tue, wed, thu, fri, sat, sun);
    
    // Выводим название клуба
    printf("%s\n", CLUB_NAME);
    
    // Выводим среднее значение с округлением до двух знаков после запятой
    printf("%.2f\n", average);
    
    return 0;
}
```

**Ключевые моменты решения:**
1. Используются директивы препроцессора `#define` для создания:
   - Макроса `AVERAGE_VISITORS` для вычисления среднего значения
   - Константы `DAYS_IN_WEEK` с количеством дней в неделе
   - Константы `CLUB_NAME` с названием клуба

2. Макрос принимает 7 параметров (посетителей за каждый день) и вычисляет среднее

3. Программа корректно обрабатывает ввод данных и выводит результат с округлением до двух знаков после запятой

Задача 16

**Тема:** Препроцессор C  
**Сложность:** Средняя (2)

**Описание:** В контексте спортивных соревнований по бегу на короткие дистанции (спринт) необходимо определить категорию спортсмена на основе его лучшего времени забега. Категории определяются макросами препроцессора. Используйте директивы условной компиляции для выбора системы категоризации (юниоры или взрослые) в зависимости от заданной константы времени компиляции. Для хранения времени спортсмена используйте тип данных `float`.

Система категорий для юниоров (определяется, если `JUNIOR_CATEGORY` равен 1):
- `CATEGORY_A`: время <= 11.5 секунд.
- `CATEGORY_B`: время > 11.5 и <= 13.0 секунд.
- `CATEGORY_C`: время > 13.0 секунд.

Система категорий для взрослых (используется, если `JUNIOR_CATEGORY` не равен 1):
- `CATEGORY_ELITE`: время <= 10.5 секунд.
- `CATEGORY_COMPETITIVE`: время > 10.5 и <= 12.0 секунд.
- `CATEGORY_AMATEUR`: время > 12.0 секунд.

Напишите программу, которая считывает лучшее время спринтера и, используя макросы и условную компиляцию, выводит его категорию в виде строки (например, "CATEGORY_A" или "CATEGORY_ELITE").

**Формат входных данных:** Одно вещественное число — лучшее время спринтера в секундах.

**Формат выходных данных:** Одна строка — название категории спортсмена в соответствии с выбранной системой.

**Пример 1 (если `JUNIOR_CATEGORY` определен как 1):**
Входные данные:
12.8
Выходные данные:
CATEGORY_B

**Пример 2 (если `JUNIOR_CATEGORY` не определен или равен 0):**
Входные данные:
10.3
Выходные данные:
CATEGORY_ELITE

---

## **Решение:**

```c
#include <stdio.h>

// Определяем константу для выбора системы категорий
// Измените значение на 1 для юниоров, на 0 или другое для взрослых
#define JUNIOR_CATEGORY 1

// Условная компиляция: выбираем набор макросов в зависимости от JUNIOR_CATEGORY
#if JUNIOR_CATEGORY == 1
    // Система категорий для юниоров
    #define CATEGORY_A(time) ((time) <= 11.5f)
    #define CATEGORY_B(time) ((time) > 11.5f && (time) <= 13.0f)
    #define CATEGORY_C(time) ((time) > 13.0f)
    
    // Макрос-функция, возвращающая строку категории для юниоров
    #define GET_CATEGORY_STR(time) \
        (CATEGORY_A(time) ? "CATEGORY_A" : \
         CATEGORY_B(time) ? "CATEGORY_B" : \
         "CATEGORY_C")
#else
    // Система категорий для взрослых (если JUNIOR_CATEGORY != 1)
    #define CATEGORY_ELITE(time) ((time) <= 10.5f)
    #define CATEGORY_COMPETITIVE(time) ((time) > 10.5f && (time) <= 12.0f)
    #define CATEGORY_AMATEUR(time) ((time) > 12.0f)
    
    // Макрос-функция, возвращающая строку категории для взрослых
    #define GET_CATEGORY_STR(time) \
        (CATEGORY_ELITE(time) ? "CATEGORY_ELITE" : \
         CATEGORY_COMPETITIVE(time) ? "CATEGORY_COMPETITIVE" : \
         "CATEGORY_AMATEUR")
#endif

int main() {
    float best_time;
    
    // Считываем лучшее время спринтера
    if (scanf("%f", &best_time) != 1) {
        printf("Ошибка ввода\n");
        return 1;
    }
    
    // Используем макрос для получения строки категории и выводим результат
    printf("%s\n", GET_CATEGORY_STR(best_time));
    
    return 0;
}
```

**Ключевые моменты решения:**
1. Используется `#define JUNIOR_CATEGORY 1` для определения константы времени компиляции
2. Применяется условная компиляция `#if JUNIOR_CATEGORY == 1` для выбора между двумя системами категорий
3. Создаются макросы-функции для проверки времени и определения категории
4. Используется тернарный оператор внутри макросов для возврата строковых значений
5. Программа считывает время и выводит соответствующую категорию

**Как тестировать:**
- Чтобы протестировать систему для юниоров, оставьте `#define JUNIOR_CATEGORY 1`
- Чтобы протестировать систему для взрослых, измените на `#define JUNIOR_CATEGORY 0` или закомментируйте эту строку

Задача 17
## **Задание: Система управления складом с выбором алгоритма упаковки**

**Описание:** В системе управления складом необходимо автоматически выбирать алгоритм упаковки товаров в зависимости от их типа и доступного пространства. Тип товара кодируется целым числом: 1 — хрупкий, 2 — сыпучий, 3 — жидкий. Доступное пространство задаётся в кубических метрах как вещественное число. Правила выбора алгоритма:
- Для хрупких товаров (тип 1) всегда используется алгоритм "PACK_FRAGILE".
- Для сыпучих (тип 2) и жидких (тип 3) выбор зависит от пространства:
    - Если пространство >= 10.0, используется алгоритм "PACK_BULK".
    - Если пространство < 10.0, используется алгоритм "PACK_COMPACT".

Реализуйте решение с помощью директив препроцессора (#define, #if, #elif, #else). Программа должна выводить только название выбранного алгоритма в виде строки.

**Формат входных данных:** Два числа, разделённых пробелом. Первое — целое число type (1, 2 или 3). Второе — вещественное число space.

**Формат выходных данных:** Одна строка — название алгоритма.

**Примеры работы:**
```
Вход: 1 5.0
Выход: PACK_FRAGILE

Вход: 2 12.5
Выход: PACK_BULK

Вход: 3 7.2
Выход: PACK_COMPACT
```

---

## **Решение:**

```c
#include <stdio.h>

// Определяем константы для типов товаров
#define TYPE_FRAGILE 1
#define TYPE_BULK    2
#define TYPE_LIQUID  3

// Определяем константы для алгоритмов упаковки
#define PACK_FRAGILE "PACK_FRAGILE"
#define PACK_BULK    "PACK_BULK"
#define PACK_COMPACT "PACK_COMPACT"

int main() {
    int type;
    double space;
    
    // Чтение входных данных
    scanf("%d %lf", &type, &space);
    
    // Выбор алгоритма с помощью директив препроцессора
    #if defined(TYPE_FRAGILE) && defined(TYPE_BULK) && defined(TYPE_LIQUID)
        // Для хрупких товаров всегда один алгоритм
        if (type == TYPE_FRAGILE) {
            printf("%s\n", PACK_FRAGILE);
        }
        // Для сыпучих и жидких выбор зависит от пространства
        else if (type == TYPE_BULK || type == TYPE_LIQUID) {
            if (space >= 10.0) {
                printf("%s\n", PACK_BULK);
            } else {
                printf("%s\n", PACK_COMPACT);
            }
        }
    #endif
    
    return 0;
}
```

**Особенности задания:**
1. Используются директивы препроцессора `#define` для создания констант
2. Применяется директива `#if defined()` для условной компиляции
3. Задание требует понимания работы с условными директивами препроцессора
4. Решение демонстрирует практическое применение препроцессора для конфигурации логики программы

Это задание сложности 3, так как требует комбинирования нескольких концепций препроцессора и создания условной логики на этапе компиляции.

Задание 18
## 📋 **Задание: Сортировка по частоте**

**Тема:** Массивы (сложность 3)

**Описание:** Вам дан массив целых чисел. Отсортируйте его так, чтобы числа располагались в порядке убывания частоты их встречаемости в исходном массиве. Если два числа имеют одинаковую частоту, то первым должно идти то число, которое меньше по значению.

**Формат входных данных:**
- Первая строка: целое число N (1 ≤ N ≤ 100) — размер массива
- Вторая строка: N целых чисел через пробел (каждое от -100 до 100)

**Формат выходных данных:**
- Одна строка с отсортированными элементами через пробел

**Примеры:**
```
Вход:
10
1 2 3 1 2 3 4 5 4 1

Выход:
1 1 1 2 2 3 3 4 4 5
```

```
Вход:
8
5 5 5 2 2 8 8 8

Выход:
5 5 5 8 8 8 2 2
```

## 💻 **Решение на языке C:**

```c
#include <stdio.h>
#include <stdlib.h>

// Структура для хранения числа и его частоты
typedef struct {
    int value;   // само число
    int freq;    // частота встречаемости
} NumFreq;

// Функция для сравнения двух элементов NumFreq при сортировке
// Сначала по частоте (убывание), затем по значению (возрастание)
int compare(const void *a, const void *b) {
    NumFreq *elem1 = (NumFreq *)a;
    NumFreq *elem2 = (NumFreq *)b;
    
    // Если частоты разные, сортируем по убыванию частоты
    if (elem1->freq != elem2->freq) {
        return elem2->freq - elem1->freq;
    }
    // Если частоты равны, сортируем по возрастанию значения
    return elem1->value - elem2->value;
}

int main() {
    int N;
    scanf("%d", &N);
    
    int arr[100]; // исходный массив
    for (int i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
    }
    
    // Массив для хранения уникальных чисел и их частот
    NumFreq freqArr[100];
    int uniqueCount = 0; // количество уникальных чисел
    
    // Подсчет частот
    for (int i = 0; i < N; i++) {
        int found = 0;
        // Проверяем, есть ли текущее число уже в массиве уникальных
        for (int j = 0; j < uniqueCount; j++) {
            if (freqArr[j].value == arr[i]) {
                freqArr[j].freq++;
                found = 1;
                break;
            }
        }
        // Если число встретилось первый раз, добавляем его
        if (!found) {
            freqArr[uniqueCount].value = arr[i];
            freqArr[uniqueCount].freq = 1;
            uniqueCount++;
        }
    }
    
    // Сортируем массив структур по заданным правилам
    qsort(freqArr, uniqueCount, sizeof(NumFreq), compare);
    
    // Выводим результат согласно отсортированным частотам
    for (int i = 0; i < uniqueCount; i++) {
        for (int j = 0; j < freqArr[i].freq; j++) {
            printf("%d ", freqArr[i].value);
        }
    }
    printf("\n");
    
    return 0;
}

Задача 19

**Тема:** Переменные и типы данных  
**Сложность:** 3 (сложное)

**Описание задачи:**
В строительном магазине действует акция: при покупке более 100 единиц одного материала предоставляется скидка 15%. Также на все покупки добавляется налог 7%. Напишите программу, которая вычисляет итоговую стоимость заказа.

**Входные данные:**
Три целых числа, каждое на новой строке:
- Цена за одну единицу материала (в рублях)
- Количество единиц материала
- Базовая стоимость доставки (в рублях)

**Выходные данные:**
Пять вещественных чисел, каждое на новой строке, с точностью до двух знаков после запятой:
1. Стоимость без скидки
2. Скидка
3. Стоимость со скидкой
4. Налог
5. Итог к оплате

**Пример:**
```
Входные данные:
150
120
500

Выходные данные:
Стоимость без скидки: 18000.00
Скидка: 2700.00
Стоимость со скидкой: 15300.00
Налог: 1071.00
Итог к оплате: 16871.00
```

## **Решение:**

```c
#include <stdio.h>

int main() {
    // Объявление переменных для входных данных
    int price_per_unit, quantity, delivery;
    
    // Считывание входных данных
    scanf("%d", &price_per_unit);
    scanf("%d", &quantity);
    scanf("%d", &delivery);
    
    // 1. Расчет стоимости товаров без скидки и налога
    double total_without_discount = price_per_unit * quantity;
    
    // 2. Расчет скидки (15% если количество > 100)
    double discount = 0.0;
    if (quantity > 100) {
        discount = total_without_discount * 0.15;
    }
    
    // 3. Стоимость товаров после применения скидки
    double total_after_discount = total_without_discount - discount;
    
    // 4. Расчет налога (7% от стоимости после скидки)
    double tax = total_after_discount * 0.07;
    
    // 5. Расчет итоговой суммы к оплате
    double final_total = total_after_discount + tax + delivery;
    
    // Вывод результатов с точностью до двух знаков после запятой
    printf("Стоимость без скидки: %.2f\\n", total_without_discount);
    printf("Скидка: %.2f\\n", discount);
    printf("Стоимость со скидкой: %.2f\\n", total_after_discount);
    printf("Налог: %.2f\\n", tax);
    printf("Итог к оплате: %.2f\\n", final_total);
    
    return 0;
}
```

**Особенности задания (сложность 3):**
1. Работа с разными типами данных (int для входных, double для расчетов)
2. Условные вычисления (скидка только при quantity > 100)
3. Последовательные вычисления с зависимостями между шагами
4. Форматированный вывод с точностью до двух знаков
5. Комбинация арифметических операций и логических условий

Задача 20


Не вывел условие, но сделал решение

Задача 21
user_request = "Создай задание на тему '9' (Динамическая память) сложности 2 и решение к нему"
## **Задание: Моделирование загрузки контейнеров на корабль**

**Тема:** Динамическая память

**Задача:** Вам необходимо смоделировать процесс загрузки контейнеров на корабль. Корабль за один рейс может принять определенное количество контейнеров, которое заранее неизвестно. Программа должна динамически выделять память под массив целых чисел (масс контейнеров) по мере их поступления. Когда поступает контейнер с массой 0 (сигнал окончания ввода), программа должна вычислить общую массу всех загруженных контейнеров и среднюю массу одного контейнера.

**Формат входных данных:** Программа считывает целые числа — массы контейнеров в тоннах. Ввод завершается, когда будет введено число 0. Гарантируется, что будет введен хотя бы один контейнер до нуля.

**Формат выходных данных:** Два вещественных числа, каждое с новой строки: общая масса всех контейнеров и средняя масса одного контейнера. Выводите числа с точностью до двух знаков после запятой.

**Пример работы:**
```
Входные данные:
5
12
8
3
0

Выходные данные:
28.00
7.00
```

**Подсказка:** Используйте `realloc` для увеличения выделенной памяти при поступлении каждого нового контейнера. Не забудьте освободить память.

---

## **Решение:**

```c
#include <stdio.h>
#include <stdlib.h>

int main() {
    int *containers = NULL; // Указатель на динамический массив масс контейнеров
    int capacity = 0;       // Текущее количество контейнеров в массиве
    int mass;               // Переменная для чтения массы очередного контейнера

    // Чтение масс контейнеров до тех пор, пока не будет введен 0
    while (1) {
        scanf("%d", &mass);
        
        // Если введен 0, завершаем ввод
        if (mass == 0) {
            break;
        }

        // Увеличиваем размер массива на один элемент
        capacity++;
        int *temp = (int*)realloc(containers, capacity * sizeof(int));
        
        // Проверяем, удалось ли выделить память
        if (temp == NULL) {
            printf("Ошибка выделения памяти\n");
            free(containers);
            return 1;
        }
        
        containers = temp;
        containers[capacity - 1] = mass; // Сохраняем массу нового контейнера
    }

    // Вычисление общей массы
    double total_mass = 0.0;
    for (int i = 0; i < capacity; i++) {
        total_mass += containers[i];
    }

    // Вычисление средней массы
    double average_mass = total_mass / capacity;

    // Вывод результатов с точностью до двух знаков после запятой
    printf("%.2f\n", total_mass);
    printf("%.2f\n", average_mass);

    // Освобождение выделенной динамической памяти
    free(containers);

    return 0;
}
```

**Ключевые моменты решения:**
1. Используется `realloc()` для динамического увеличения массива при каждом новом контейнере
2. Проверяется успешность выделения памяти
3. Память освобождается с помощью `free()` в конце программы
4. Результаты выводятся с точностью до двух знаков после запятой
5. Обработка ошибок при выделении памяти

Задача 22

**Тема:** Динамическая память  
**Название:** Хранение данных о количестве туристов

**Описание задания:**
Вы работаете в туристическом агентстве и вам нужно хранить данные о количестве туристов в разных группах. Напишите программу, которая динамически выделяет память для хранения целого числа (количество туристов в одной группе), считывает это число с клавиатуры, выводит его на экран и затем освобождает выделенную память.

**Формат входных данных:**
Одно целое число — количество туристов в группе.

**Формат выходных данных:**
Выведите введенное число.

**Пример работы:**
```
Входные данные:
15
Выходные данные:
15
```

**Подсказка:** Используйте функцию malloc для выделения памяти и free для её освобождения.

---

## Решение задания

```c
#include <stdio.h>   // Для функций ввода/вывода (printf, scanf)
#include <stdlib.h>  // Для функций динамического выделения памяти (malloc, free)

int main() {
    int *tourists; // Указатель для хранения адреса выделенной памяти

    // Выделяем память под одно целое число
    tourists = (int*)malloc(sizeof(int));
    
    // Проверяем, успешно ли выделена память
    if (tourists == NULL) {
        printf("Ошибка выделения памяти\n");
        return 1; // Завершаем программу с кодом ошибки
    }

    // Считываем количество туристов с клавиатуры
    scanf("%d", tourists);

    // Выводим введенное число на экран
    printf("%d\n", *tourists);

    // Освобождаем выделенную память
    free(tourists);

    return 0; // Успешное завершение программы
}

Задача 23
## **Задание: Динамическая память (сложность 3)**

**Описание:**  
Вам необходимо реализовать функцию, которая динамически создает и заполняет двумерный массив (матрицу) для хранения данных о температуре в разных городах в течение недели. Каждая строка матрицы соответствует одному городу, каждый столбец — дню недели (с понедельника по воскресенье). Программа должна:
1.  Запросить у пользователя количество городов (N) и выделить память под массив указателей на строки (float**).
2.  Для каждого города выделить память под 7 значений температуры (float*).
3.  Заполнить матрицу значениями, введенными пользователем.
4.  Найти и вывести для каждого города среднюю температуру за неделю.
5.  Освободить всю выделенную память в обратном порядке.

**Формат входных данных:**  
Первая строка содержит целое число N (1 ≤ N ≤ 100) — количество городов.  
Далее следует N строк, в каждой из которых через пробел записаны 7 вещественных чисел — температуры с понедельника по воскресенье для соответствующего города.

**Формат выходных данных:**  
Выведите N строк. В i-й строке выведите среднюю температуру для i-го города с точностью до двух знаков после запятой.

**Пример работы:**  
Входные данные:
```
3
22.5 23.1 24.0 22.8 25.2 26.0 24.5
19.0 18.5 20.1 19.8 21.0 22.3 20.5
15.0 14.8 16.2 15.5 17.0 18.1 16.8
```
Выходные данные:
```
24.01
20.17
16.20
```

---

## **Решение:**

```c
#include <stdio.h>
#include <stdlib.h>

#define DAYS_IN_WEEK 7

int main() {
    int n; // Количество городов
    float **temperatures; // Двумерный массив для температур
    float sum, average;

    // Ввод количества городов
    scanf("%d", &n);

    // 1. Выделение памяти под массив указателей на строки
    temperatures = (float **)malloc(n * sizeof(float *));
    if (temperatures == NULL) {
        printf("Ошибка выделения памяти\n");
        return 1;
    }

    // 2. Выделение памяти для каждой строки (7 значений температуры)
    for (int i = 0; i < n; i++) {
        temperatures[i] = (float *)malloc(DAYS_IN_WEEK * sizeof(float));
        if (temperatures[i] == NULL) {
            printf("Ошибка выделения памяти\n");
            // Освобождаем уже выделенную память перед выходом
            for (int j = 0; j < i; j++) {
                free(temperatures[j]);
            }
            free(temperatures);
            return 1;
        }
    }

    // 3. Заполнение матрицы значениями
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < DAYS_IN_WEEK; j++) {
            scanf("%f", &temperatures[i][j]);
        }
    }

    // 4. Вычисление и вывод средней температуры для каждого города
    for (int i = 0; i < n; i++) {
        sum = 0;
        for (int j = 0; j < DAYS_IN_WEEK; j++) {
            sum += temperatures[i][j];
        }
        average = sum / DAYS_IN_WEEK;
        printf("%.2f\n", average);
    }

    // 5. Освобождение выделенной памяти в обратном порядке
    for (int i = 0; i < n; i++) {
        free(temperatures[i]);
    }
    free(temperatures);

    return 0;
}

Задача 24
## **Задание: Управление бюджетом путешествия**

**Описание:** Вам необходимо реализовать программу для управления бюджетом путешествия. Программа должна динамически выделять память под массив расходов (тип float) и позволять пользователю добавлять новые расходы, удалять последний добавленный расход и выводить текущий список расходов с общим итогом. Количество расходов заранее неизвестно и определяется вводом пользователя. Программа должна корректно освобождать всю выделенную память.

**Формат входных данных:**  
Первое число — начальное количество расходов N (0 ≤ N ≤ 1000). Затем вводятся N чисел типа float — суммы расходов.  
Далее идут команды, каждая в отдельной строке:  
- `add <сумма>` — добавить новый расход (число float).  
- `pop` — удалить последний добавленный расход.  
- `print` — вывести все текущие расходы и общую сумму.  
- `exit` — завершить программу.  

**Формат выходных данных:**  
При команде `print` выведите в одной строке через пробел все текущие расходы с двумя знаками после запятой, а на следующей строке — общую сумму с двумя знаками после запятой и надписью "Total: ". Если список расходов пуст, выведите "No expenses" и на следующей строке "Total: 0.00".

**Пример работы:**  
Входные данные:  
3  
120.5 45.75 300.0  
add 50.25  
print  
pop  
print  
exit  

Выходные данные:  
120.50 45.75 300.00 50.25  
Total: 516.50  
120.50 45.75 300.00  
Total: 466.50  

**Подсказка:** Используйте динамический массив и перевыделяйте память при добавлении/удалении элементов, отслеживая текущий размер и вместимость массива.

---

## **Решение:**

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    int capacity = 10; // Начальная вместимость массива
    float *expenses = (float *)malloc(capacity * sizeof(float));
    if (expenses == NULL) {
        printf("Memory allocation failed\n");
        return 1;
    }

    int size = 0; // Текущее количество расходов
    int initial_n;
    scanf("%d", &initial_n);

    // Чтение начальных расходов
    for (int i = 0; i < initial_n; i++) {
        if (size >= capacity) {
            capacity *= 2;
            float *temp = (float *)realloc(expenses, capacity * sizeof(float));
            if (temp == NULL) {
                printf("Memory reallocation failed\n");
                free(expenses);
                return 1;
            }
            expenses = temp;
        }
        scanf("%f", &expenses[size]);
        size++;
    }

    char command[10];
    float value;
    
    // Обработка команд
    while (1) {
        scanf("%s", command);
        
        if (strcmp(command, "add") == 0) {
            scanf("%f", &value);
            
            // Увеличение вместимости при необходимости
            if (size >= capacity) {
                capacity *= 2;
                float *temp = (float *)realloc(expenses, capacity * sizeof(float));
                if (temp == NULL) {
                    printf("Memory reallocation failed\n");
                    free(expenses);
                    return 1;
                }
                expenses = temp;
            }
            
            expenses[size] = value;
            size++;
        }
        else if (strcmp(command, "pop") == 0) {
            if (size > 0) {
                size--;
                
                // Уменьшение вместимости для экономии памяти
                if (size < capacity / 4 && capacity > 10) {
                    capacity /= 2;
                    float *temp = (float *)realloc(expenses, capacity * sizeof(float));
                    if (temp == NULL) {
                        printf("Memory reallocation failed\n");
                        free(expenses);
                        return 1;
                    }
                    expenses = temp;
                }
            }
        }
        else if (strcmp(command, "print") == 0) {
            if (size == 0) {
                printf("No expenses\n");
                printf("Total: 0.00\n");
            } else {
                float total = 0;
                for (int i = 0; i < size; i++) {
                    printf("%.2f ", expenses[i]);
                    total += expenses[i];
                }
                printf("\nTotal: %.2f\n", total);
            }
        }
        else if (strcmp(command, "exit") == 0) {
            break;
        }
    }

    // Освобождение памяти
    free(expenses);
    
    return 0;
}

Задача 25

## Задание: Расчет стоимости строительства фундамента

**Описание задачи:**
Программа должна вычислять общую стоимость бетона для фундамента сложной формы, состоящего из трех прямоугольных секций разной высоты. Все размеры задаются в сантиметрах, стоимость бетона — в рублях за кубический метр.

**Входные данные:** 8 чисел с плавающей точкой (тип `float`), разделенных пробелами: `L W H1 H2 L3 W3 H3 Price`

**Выходные данные:** 3 числа, каждое с новой строки:
1. Общий объем в куб. м, с точностью ровно до трех знаков после запятой
2. Общая стоимость, с точностью ровно до двух знаков после запятой
3. Целое число — процент объема третьей секции

## Решение:

```c
#include <stdio.h>
#include <math.h>

int main() {
    // Объявление переменных для входных данных
    float L, W, H1, H2, L3, W3, H3, Price;
    
    // Считывание входных данных
    scanf("%f %f %f %f %f %f %f %f", &L, &W, &H1, &H2, &L3, &W3, &H3, &Price);
    
    // Расчет объемов каждой секции в кубических метрах
    // Перевод из см³ в м³: деление на 1 000 000
    float volume1 = (L * W * H1) / 1000000.0f;
    float volume2 = (L * W * H2) / 1000000.0f;
    float volume3 = (L3 * W3 * H3) / 1000000.0f;
    
    // Общий объем бетона
    float total_volume = volume1 + volume2 + volume3;
    
    // Общая стоимость бетона
    float total_cost = total_volume * Price;
    
    // Расчет процента объема третьей секции
    // Умножаем на 100 для получения процентов, округляем математически
    int percentage = (int)round((volume3 / total_volume) * 100.0f);
    
    // Вывод результатов с заданной точностью
    printf("%.3f\\n", total_volume);   // Объем с тремя знаками после запятой
    printf("%.2f\\n", total_cost);     // Стоимость с двумя знаками после запятой
    printf("%d\\n", percentage);       // Процент как целое число
    
    return 0;
}







